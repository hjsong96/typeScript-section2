// object
let user: {
    id?: number;
    name: string;
} = {
    id: 1,
    name: '송송송',
};

let config: {
    readonly apiKey: string;
} = {
    apiKey: 'my api key',
};

// config.apiKey = 'hacked';
